# This is a journal config file

# Journal base directory
export JOURNAL="$HOME/Documents/journal"

# Make new journal entry
entry () {
	mkdir -p "$JOURNAL/$(date +%Y/%b/)"
	touch "$_$(date +%d)"
	e "$_"
}
